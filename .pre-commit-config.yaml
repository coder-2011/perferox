repos:
  - repo: local
    hooks:
      - id: tests
        name: tests
        entry: uv run python -m unittest tests.py
        language: system
        files: ^(perferox/|tests\.py$|pyproject\.toml$|uv\.lock$)
        pass_filenames: false
